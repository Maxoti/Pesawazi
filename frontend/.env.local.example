# URL of the pesawazi backend (see ../backend)
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000

# Must match DASHBOARD_API_KEY in the backend's .env
NEXT_PUBLIC_DASHBOARD_API_KEY=replace-with-a-long-random-string
